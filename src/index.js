import React from 'react';

import './index.css';

import ReactDOM from 'react-dom';
import MainApp from './App'; // Update the import statement



ReactDOM.render(
  <React.StrictMode>
    <MainApp />
  </React.StrictMode>,
  document.getElementById('root')
);
